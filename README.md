# neovim configuration

### Installation instructions
```sh
git clone git@github.com:Kindlewing/neovim-config.git ~/.config/nvim/
```
```sh
nvim
```
Everything should work. If not, run ``` :PackerSync ``` from within neovim
